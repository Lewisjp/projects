source 'https://rubygems.org'

gem 'sinatra', require: 'sinatra/base'

	group :development do
		gem "shotgun"  #allows us to not have to restart server
		gem "tux"  #debugger for the app, alows interactivity 
	end